当下任务：
1. ~~写commit message~~
1. 简历更新，讲一下新文章和架构演进能力
2. docker，挂载，不要再用多阶段构建了
3. P99抖动问题仍然存在，上次20轮测试后现在又抖动了，继续排查调度不均问题
4. 错误处理catch的函数好像不对
5. 设计Buffer缓冲区内部实现，用更好的方案代替string
6. 缓冲区的拷贝开销也有点大了，内核缓冲区→临时缓冲区一次，临时缓冲区→单链接缓冲区又一次
7. function vs 函数指针回调的性能测试
8. 清除冗余的日志
9. ~~继续拆handleClient，recv和send的逻辑不该呆在一起~~
10. Select瓶颈打满（哪个瓶颈）
11. ~~epoll ET vs epoll LT vs io_uring到底选哪个→io_uring~~
13. 建立可复用的提示词框架
14. ~~eventLoop类拆出来，作为薄薄一层~~
15. 后期把map管理conn替换为智能指针管理
16. 画架构图，用mermaid
17. 复习一下accept()阻塞原理与唤醒机制，并结合新技术进行阅读（零拷贝、DPDK）
18. 如何区分连接数和并发数，为什么wrk测试下select可以突破1024连接
19. 产出《多路复用：Select瓶颈分析》收录于网络编程
20. 产出文章《架构演进: 如何把一个黑盒服务器一步步拆成简易网络库》收录于网络编程
```
- 文章大纲如下：
1. 确定产品定位：灵活性高、组件多、黑盒少 
2. 发现最大矛盾————上帝类 
3. 确定目标：希望我的底层完全不依靠tcpserver底层组件也能用起来，但是Tcpserver依赖底层运转，形成依赖倒置，所以我要做的是将这个库拆到完全没有任何一个上帝类也能跑起来的程度。
4. 开发与重构,在过度设计 vs 清晰简洁职责中做权衡。
5. 展示commit message
```
21. 改名，takeData→take，不对，再思考一下。
22. 拆分handleClient，拆分完之后把recv、send分配给connection而不是eventloop，eventloop只需要调用回调、recv、send
23. 把所有空的构造/析构函数都写成default

1. 用长连接打出1024限制
2. 将IO模型替换为epoll ET，显示O(n)的劣势